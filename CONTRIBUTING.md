# 開発ガイド

環境構築は [README](README.md#開発) を参照してください。

## 変更を加えるとき

- UIのマークアップは `src/ui/editor.ts`、描画処理は `src/renderer.ts` に置きます。
- 座標は原則として動画の幅・高さに対する0〜1の値で保存します。
- Rustの `models.rs` とTypeScriptの `types.ts` がTauriコマンドの入出力です。変更時は両方を確認してください。
- `boxes` の既存JSONとの互換性を維持します。旧形式の矩形 `box` も読み込めます。
- ユーザーの動画・選択範囲・出力やローカルパス、生成済みEXEをコミットしないでください。
- パッケージはpnpm、Rust依存はCargoで更新し、それぞれのlockfileもコミットします。

## 整形と検証

`pnpm format` と `cargo fmt --manifest-path src-tauri/Cargo.toml` で整形します。
PR前にREADMEのチェックコマンドを実行してください。Windows CIでも型・整形・テストを検証します。

Nodeのテストはズームの座標保持、一括処理の対象判定・順次実行・キャンセルを検証します。
Rustのテストは保存先、更新状態、回転情報、実際のFFmpeg書き出しを検証します。
`--include-ignored` を指定する実動画テストは、先に `pnpm setup:ffmpeg` が必要です。

UI変更時は通常サイズ（1180×760）と最小サイズ（760×520）の両方で、
メイン画面にページスクロールが発生しないこと、フォーカスと選択状態が分かることを確認してください。

## 不具合報告

操作手順、期待した動作、実際の動作、Windowsとアプリのバージョンを添えてください。
動画は必要な場合だけ、共有できる短い再現用データを使用してください。

寄稿した変更は、本体のMITライセンスで提供する前提です。
