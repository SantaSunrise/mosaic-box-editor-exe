{
  "name": "mosaic-box-editor",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit",
    "test": "node --test tools/*.test.mjs",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "pnpm check && vite build",
    "tauri": "tauri",
    "setup:ffmpeg": "pwsh -NoProfile -File tools/setup-ffmpeg.ps1",
    "package:portable": "pwsh -NoProfile -File tools/package-portable.ps1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "@tauri-apps/plugin-dialog": "^2.4.0",
    "lucide": "^1.47.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "prettier": "3.6.2",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  },
  "license": "MIT",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22.18.0"
  }
}
